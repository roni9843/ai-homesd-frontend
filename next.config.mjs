/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    domains: [
      "picsum.photos",
      "api.lorem.space",
      "m.media-amazon.com",
      "i.ibb.co",
      "tenor.com",
      "backend.aihomesd.com",
    ],
  },
};

export default nextConfig;
